import styled from "styled-components";

export const ContainerHeader = styled.div`
  background-color: var(--primary);
  box-shadow: 0 0.125rem 0.125rem 0;
  height: 100px;
`;

export const ImgLogo = styled.img`
  width: auto;
  margin: 15px;
  max-height: 70px;
`;